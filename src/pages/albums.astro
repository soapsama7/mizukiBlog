---
import AzaMOELayout from "../layouts/AzaMOELayout.astro";
import { scanAlbums } from "../utils/album-scanner";
import { themeTextConfig } from "../config";

// 获取所有相册
const albumsData = await scanAlbums();
---

<AzaMOELayout current="albums" title={themeTextConfig.pages.albums.title}>
	<section class="corner-blog">
		<h2>{themeTextConfig.pages.albums.title}</h2>
		<div class="corner-subtitle">{themeTextConfig.pages.albums.subtitle}</div>
		<div class="corner-grid">
			{
				albumsData.map((album) => (
					<article class="corner-card">
						<a href={`/albums/${album.id}/`}>
							<img class="corner-album-cover" src={album.cover} alt={album.title} loading="lazy" />
						</a>
						<a class="corner-title" href={`/albums/${album.id}/`}>
							{album.title}
						</a>
						<div class="corner-excerpt">{album.description || "暂无描述"}</div>
					</article>
				))
			}
		</div>
	</section>
</AzaMOELayout>